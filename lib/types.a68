
MODE POS = STRUCT( INT x, y );
MODE SIZE = STRUCT( INT w, h );
MODE POSORINT = UNION(POS, INT);

MODE PIXEL = STRUCT(
  STRING value,
  INT x, y
);

MODE SCHEME = FLEX [0] PIXEL;
MODE SCHEMES = FLEX [0] SCHEME
